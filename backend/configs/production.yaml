server:
  host: "0.0.0.0"
  port: "8080"

database:
  host: "db"
  port: "5432"
  username: "facego"
  password: "changeme"
  database: "facego"
  ssl_mode: "require"

log:
  level: "warn"

storage:
  uploads_dir: "/var/facego/uploads"
